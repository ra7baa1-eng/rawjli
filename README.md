# روجلي Rawjli - منصة التسويق بالعمولة

منصة احترافية للتسويق بالعمولة موجهة للسوق الجزائري، مبنية بتقنيات حديثة ومصممة خصيصاً لتكون بسيطة وسهلة الاستخدام.

## 🎯 المميزات

### للأدمن
- إدارة كاملة للمنتجات والفئات والعروض والباقات
- نظام متغيرات مرن (ألوان، مقاسات، أشكال)
- إدارة الطلبيات وتغيير حالاتها
- إدارة المسوقين (إضافة، تفعيل، تعطيل)
- تحديد أسعار التوصيل لكل ولاية
- إدارة طلبات السحب والموافقة عليها
- تقارير وإحصائيات شاملة

### للمسوق
- عرض المنتجات المتاحة مع أدوات التسويق
- نسخ العنوان والوصف التسويقي بنقرة واحدة
- إنشاء طلبيات جديدة بسهولة
- تحديد العمولة لكل طلبية
- متابعة الطلبيات والأرباح
- طلب سحب الأرباح (بريدي موب، CCP، شحن هاتف)
- تحديث معلومات الحساب

## 🛠️ التقنيات المستخدمة

- **Frontend**: Next.js 14 (App Router)
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL (Supabase)
- **ORM**: Prisma
- **Authentication**: NextAuth.js (Credentials)
- **Styling**: Tailwind CSS
- **Language**: TypeScript

## 📋 المتطلبات

- Node.js 18+ 
- PostgreSQL Database (Supabase)
- npm أو yarn

## 🚀 التثبيت والإعداد

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd rawjli
```

### 2. تثبيت المكتبات
```bash
npm install
```

### 3. إعداد قاعدة البيانات

أنشئ حساب على [Supabase](https://supabase.com) واحصل على رابط قاعدة البيانات.

### 4. إعداد ملف البيئة

انسخ `.env.example` إلى `.env`:
```bash
cp .env.example .env
```

عدّل الملف وأضف المعلومات التالية:
```env
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"
```

لتوليد `NEXTAUTH_SECRET`:
```bash
openssl rand -base64 32
```

### 5. إعداد قاعدة البيانات

```bash
# إنشاء الجداول
npm run db:push

# تعبئة البيانات الأولية (Admin + الولايات والبلديات)
npm run db:seed
```

### 6. تشغيل المشروع

```bash
# للتطوير
npm run dev

# للإنتاج
npm run build
npm start
```

المنصة ستكون متاحة على: `http://localhost:3000`

## 👤 حساب الأدمن الافتراضي

بعد تشغيل `npm run db:seed`:

- **البريد الإلكتروني**: `alumabdo0@gmail.com`
- **كلمة المرور**: `abdo@154122`

⚠️ **مهم**: غيّر كلمة المرور فوراً بعد أول تسجيل دخول!

## 📁 هيكلة المشروع

```
rawjli/
├── app/                    # Next.js App Router
│   ├── api/               # API Routes
│   ├── admin/             # صفحات الأدمن
│   ├── dashboard/         # صفحات المسوق
│   └── login/             # صفحة تسجيل الدخول
├── components/            # مكونات React
├── lib/                   # مكتبات مساعدة
├── prisma/               # Prisma Schema + Seed
├── types/                # TypeScript Types
└── algeria_cities.json   # بيانات الولايات والبلديات
```

## 🗄️ قاعدة البيانات

### الجداول الرئيسية
- `User` - المستخدمين (Admin/Marketer)
- `Product` - المنتجات
- `ProductOption` - خيارات المنتج (لون، مقاس، إلخ)
- `ProductOptionValue` - قيم الخيارات
- `Category` - الفئات
- `Offer` - العروض
- `Bundle` - الباقات
- `Order` - الطلبيات
- `OrderItem` - عناصر الطلبية
- `Wilaya` - الولايات
- `Commune` - البلديات
- `ShippingPrice` - أسعار التوصيل
- `Withdrawal` - طلبات السحب
- `Referral` - نظام الإحالات (غير مفعّل)

## 🌐 النشر

### Vercel (Frontend)
1. ادفع الكود إلى GitHub
2. اربط المشروع مع Vercel
3. أضف متغيرات البيئة
4. انشر المشروع

### Render (Backend/Cron)
استخدم Render لأي مهام خلفية أو Cron Jobs مستقبلية.

### Supabase (Database)
قاعدة البيانات جاهزة على Supabase.

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- JWT Sessions عبر NextAuth
- Middleware للتحقق من الصلاحيات
- حماية API Routes

## 📝 ملاحظات مهمة

### نظام المتغيرات (Variants)
- نظام مرن بالكامل - لا أعمدة ثابتة
- يمكن إضافة أي نوع من الخيارات (لون، مقاس، شكل، إلخ)
- المسوق يختار القيم عند إنشاء الطلب

### العمولات
- المسوق يحدد العمولة بنفسه لكل طلبية
- العمولة تُضاف للرصيد فقط عند تغيير حالة الطلب إلى "تم التوصيل"
- الرصيد القابل للسحب منفصل عن الرصيد الإجمالي

### نظام الإحالات
- البنية موجودة في قاعدة البيانات
- غير مفعّل حالياً
- يمكن تفعيله لاحقاً

## 🚫 القيود

- ❌ لا يوجد دفع إلكتروني (Stripe/PayPal)
- ❌ لا يوجد نظام هرمي
- ✅ نظام مبيعات فقط

## 📞 الدعم

للمساعدة أو الاستفسارات، تواصل مع المطور.

## 📄 الترخيص

جميع الحقوق محفوظة © 2024 Rawjli
