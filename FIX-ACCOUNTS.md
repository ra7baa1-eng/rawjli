# إصلاح مشكلة الحسابات - Fix Accounts Issue

## المشكلة
بعد تغيير نظام المصادقة من `auth-simple` إلى `auth`، جميع الحسابات لا تعمل لأن:
1. الحسابات القديمة قد تكون بكلمات مرور غير مشفرة
2. أو لا توجد حسابات في قاعدة البيانات

## الحل السريع

### الطريقة 1: إنشاء حساب Admin جديد
```bash
npm run create-admin
```

هذا سينشئ/يحدث حساب الأدمن:
- **البريد**: `alumabdo0@gmail.com`
- **كلمة المرور**: `abdo@154122`

### الطريقة 2: إعادة تعيين جميع كلمات المرور
```bash
npm run reset-passwords
```

هذا سيعيد تعيين جميع كلمات المرور إلى: `123456`

⚠️ **مهم**: بعد تسجيل الدخول، غيّر كلمة المرور فوراً!

### الطريقة 3: استخدام Seed Script
```bash
npm run db:seed
```

هذا سينشئ:
- حساب الأدمن
- جميع الولايات والبلديات
- أسعار التوصيل الافتراضية

## إنشاء حساب جديد

### للمسوقين:
1. اذهب إلى `/register`
2. املأ البيانات
3. سيتم إنشاء الحساب بكلمة مرور مشفرة

### للأدمن:
استخدم أحد الطرق أعلاه

## التحقق من الحسابات

```sql
-- في قاعدة البيانات
SELECT email, role, "isActive" FROM "User";
```

## ملاحظات

- جميع كلمات المرور الآن مشفرة باستخدام bcrypt
- لا يمكن تسجيل الدخول إلا بكلمة المرور الصحيحة
- تأكد من أن قاعدة البيانات متصلة

---

**تاريخ الإصلاح:** ديسمبر 2024

