# المساهمة في منصة روجلي

## 🔧 إعداد بيئة التطوير

### المتطلبات
- Node.js 18+
- PostgreSQL أو Supabase
- npm أو yarn

### خطوات الإعداد

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd rawjli
```

2. **تثبيت المكتبات**
```bash
npm install
```

3. **إعداد قاعدة البيانات**
```bash
cp .env.example .env
# عدّل .env وأضف DATABASE_URL
npm run db:push
npm run db:seed
```

4. **تشغيل المشروع**
```bash
npm run dev
```

## 📝 معايير الكود

### TypeScript
- استخدم TypeScript في جميع الملفات
- تجنب `any` قدر الإمكان
- أضف types للـ props والـ state

### React
- استخدم Functional Components
- استخدم Hooks بدلاً من Class Components
- اتبع مبدأ Single Responsibility

### Styling
- استخدم Tailwind CSS فقط
- لا تضف CSS مخصص إلا للضرورة
- اتبع نظام الألوان الموحد

### API Routes
- تحقق من الصلاحيات دائماً
- استخدم try-catch للأخطاء
- أرجع status codes صحيحة

## 🧪 الاختبار

```bash
# Lint
npm run lint

# Build
npm run build

# Type Check
npx tsc --noEmit
```

## 📦 إضافة ميزة جديدة

1. أنشئ branch جديد
```bash
git checkout -b feature/feature-name
```

2. اكتب الكود واختبره
3. Commit مع رسالة واضحة
```bash
git commit -m "feat: add new feature"
```

4. Push وافتح Pull Request
```bash
git push origin feature/feature-name
```

## 🐛 الإبلاغ عن مشكلة

افتح Issue جديد مع:
- وصف واضح للمشكلة
- خطوات إعادة إنتاج المشكلة
- Screenshots إن أمكن
- معلومات البيئة (OS, Browser, etc.)

## 📌 ملاحظات

- اقرأ الكود الموجود قبل البدء
- اسأل إذا كان هناك شيء غير واضح
- اتبع نفس أسلوب الكود الموجود
- اختبر جيداً قبل الـ commit

شكراً لمساهمتك! 🙏
